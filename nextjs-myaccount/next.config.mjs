/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn1.iconfinder.com', 'cdn4.iconfinder.com', 'localhost'],
  },
}

export default nextConfig
